openapi: 3.0.0
info:
  title: '{{title}}'
  version: '1.0'
paths:
  /test:
    get:
      summary: Default value does not match the param type
      operationId: fakeapi.hello.test_default_missmatch_definition
      responses:
        '200':
          description: OK
      requestBody:
        description: Warning test
        content:
          application/json:
            schema:
              x-body-name: test_body
              type: object
              properties:
                test:
                  type: string
                  default: 'test'
                test2:
                  type: string
                  default: 'test2'
servers:
  - url: /v1.0
